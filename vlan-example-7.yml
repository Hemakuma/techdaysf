---
-   name: Show vlan
    hosts: leafs
    connection: local
    gather_facts: no

    tasks:
        - name: Obtaining device credentials
          include_vars: credentials.yml

        - name: run show vlan
          nxos_command:
            commands:
                - command: show vlan
                  output: json
            provider: "{{ creds }}"
